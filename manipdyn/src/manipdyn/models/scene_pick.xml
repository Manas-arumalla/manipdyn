<mujoco model="ur5e_pick">
  <compiler meshdir="ur5e_model/assets"/>
  <include file="ur5e_model/ur5e_with_gripper.xml"/>

  <statistic center="0.25 0.2 0.35" extent="0.9"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="150" elevation="-22"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3" markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 1.5" dir="0 0 -1" directional="true"/>
    <geom name="floor" size="0 0 .05" type="plane" material="groundplane"/>

    <!-- Graspable object: a wide, heavy base (stable when placed) with a thin
         post the gripper grips near the top (~0.35 m, a comfortable height
         where the arm doesn't contort into the floor). Low centre of mass ->
         it stays upright on release; no support stand to collide with. -->
    <body name="object" pos="0.45 0.0 0.0">
      <freejoint name="object_free"/>
      <geom name="object_base" type="box" pos="0 0 0.04" size="0.05 0.05 0.04"
            rgba="0.9 0.35 0.1 1" mass="0.18" friction="2.0 0.05 0.001"/>
      <geom name="object_geom" type="box" pos="0 0 0.24" size="0.013 0.013 0.16"
            rgba="0.95 0.6 0.2 1" mass="0.02" friction="2.0 0.05 0.001"/>
    </body>

    <!-- Place target (visual only). -->
    <site name="place_marker" pos="0.0 0.45 0.02" size="0.05 0.001" rgba="0.1 0.8 0.2 0.5" type="cylinder"/>
    <site name="target_marker" pos="0.45 0.0 0.4" size="0.02" rgba="1 0 0 0" type="sphere"/>
  </worldbody>

  <equality>
    <weld name="grasp" body1="gripper_base" body2="object" active="false"/>
  </equality>
</mujoco>
